fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.6.0
sqlalchemy==2.0.36
pymysql==1.1.1
pypinyin==0.55.0

# RAG 向量检索
chromadb==1.2.2
httpx==0.27.2

# 浏览器抓取（Playwright，小红书等登录墙站点）
# 有 Chrome/Edge 无需 playwright install；否则执行 playwright install chromium
# 需支持 Python 3.13 的版本（1.47.0 的 greenlet 依赖在 3.13 上无预编译轮子）
playwright==1.62.0

# LLM（可选 Anthropic 供应商，AI 采集摘要）
anthropic==0.116.0
