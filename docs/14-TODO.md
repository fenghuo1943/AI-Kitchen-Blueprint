# 实施 TODO

状态：`[ ]` 未开始，`[-]` 进行中，`[x]` 完成，`[!]` 阻塞。完成项保留历史，并在末尾补充完成日期、关联变更和验收证据。

## Phase 0：工程基线

- [x] 初始化后端与前端目录结构，完成统一配置、环境变量模板、日志与健康检查。
- [x] 完成数据库迁移脚本、基础实体定义与测试数据初始化。
- [x] 建立格式化、静态检查、单元测试与集成测试脚手架。
- [x] 输出本地启动说明，确保开发者可在本机启动服务并验证健康接口。

## Phase 1：知识库与入库

- [x] 建立食材规范化与别名映射规则，支持统一识别与检索。
- [x] 实现菜谱草稿、审核、发布与版本管理流程。
- [ ] 支持 CSV/JSON/人工录入等入库方式，并输出校验报告。
- [ ] 实现入库任务状态追踪、失败原因展示与可重试机制。
- [ ] 发布后自动触发向量索引构建，并支持重建与状态查看。

## Phase 2：库存与推荐

- [x] 实现库存 CRUD、保质期提示与库存状态管理。
- [ ] 完成硬约束过滤：过敏原、忌口、设备、时长、季节约束。
- [x] 实现食材覆盖率、缺料计算、可配置排序与推荐理由生成。
- [ ] 完成推荐 API 与前端页面联调，展示命中食材、缺料和”为什么推荐”。

## Phase 3：RAG 问答与 AI 增强

- [ ] 基于已发布菜谱实现知识库问答接口。
- [ ] 支持证据引用、无依据拒答和模型不可用降级。
- [ ] 接入本地模型或 Ollama，并完成结构化输出校验。
- [ ] 增加过敏冲突、提示词注入、无网络场景的回归测试。

## Phase 4：发布准备

- [ ] 补齐权限、审计、备份恢复与数据导出机制。
- [ ] 完成部署、运维、隐私与可访问性验收文档。
- [ ] 形成可复现的验收记录与已知限制说明。

## 完成记录

### Phase 0 完成 (2026-08-03)

- 实现了完整的项目配置管理（`app/core/config.py`）
- 创建了数据库模型和迁移脚本（`app/db/models.py`, `migrations/001_init.sql`）
- 实现了日志系统（`app/core/logging.py`）
- 创建了健康检查接口
- 建立了测试脚手架（`tests/conftest.py`, `tests/test_health.py`）
- 创建了种子数据初始化脚本（`scripts/init_seed_data.py`）
- 编写了启动脚本（`start.sh`, `start.bat`）

### Phase 1 部分完成 (2026-08-03)

- 实现了食材管理模块（CRUD + 别名管理）
- 实现了菜谱管理模块（CRUD + 发布 + 版本管理）
- 创建了完整的单元测试

### Phase 2 部分完成 (2026-08-03)

- 实现了库存管理模块（CRUD + 保质期提示 + 家庭管理）
- 实现了推荐引擎（基于规则的推荐 + 评分算法）
- 创建了推荐理由生成

### Phase 1 追加完成 (2026-08-03)

- 实现了入库任务管理模块（人工录入 + URL来源）
- 支持任务状态追踪和错误处理
- 实现了去重检查（基于来源哈希）

### Phase 2 追加完成 (2026-08-03)

- 实现了硬约束过滤（过敏原、设备、时长、季节）
- 完善了推荐引擎的过滤逻辑

### Phase 3 前端完成 (2026-08-03)

- 创建了完整的前端应用框架（Vue 3 + Vue Router + Pinia）
- 实现了首页、菜谱库、菜谱详情页面
- 实现了库存管理页面（支持家庭选择、保质期提醒）
- 实现了智能推荐页面（支持多种筛选条件）
- 实现了食材管理页面
- 配置了 API 代理和开发服务器

### 响应式设计完成 (2026-08-03)

- 创建了 `useResponsive` 组合式函数，提供设备类型检测
- 优化了全局导航栏（移动端汉堡菜单 + 侧边栏导航）
- 优化了所有页面的响应式布局：
  - 首页：移动端2列网格，桌面端4列网格
  - 菜谱库：移动端筛选器垂直堆叠，模态框底部弹出
  - 库存管理：移动端家庭选择器垂直排列
  - 智能推荐：移动端表单垂直布局
  - 食材管理：移动端筛选器垂直堆叠
  - 菜谱详情：移动端元信息垂直排列
- 添加了触摸友好设计（按钮最小高度44px，输入框16px字体）
- 更新了前端规范、项目结构规范、编码规范、开发流程、验收测试规范

## 文档完善记录

- [x] 2026-08-03：将初始纲要扩展为可执行的产品、架构、数据、RAG、推荐、采集和验收规范；新增 `16`、`17` 文档。

## cook 参考项目功能移植完成记录 (2026-08-03)

参考 [19-开发计划-优化.md](19-开发计划-优化.md)，将 `参考/cook` 菜谱网站的核心能力移植到 FastAPI + Vue 架构：

- [x] **分类体系**：菜谱/食材/调料三类分类（`categories` API，`?type=` 分发），默认分类保护，重名/被引用保护。
- [x] **调料管理**：`seasonings` API，拼音自动生成，分类关联。
- [x] **菜谱搜索升级**：食材筛选（exact 全含 / any 任一）、菜谱分类筛选、拼音前缀搜索、综合评分（title×6/分类×2/食材×2/简介×2/拼音×1 + 做过×0.5 + 收藏×2）、5 种排序（score/date/title/cook/random）。
- [x] **菜谱完整编辑**：创建/更新支持 分类/食材/调料/步骤/标签 删了重建，事务化保存；封面 URL；拼音自动生成。
- [x] **收藏**：`favorites` API，UNIQUE 幂等去重，按家庭隔离，收藏列表。
- [x] **浏览历史**：`history` API，upsert 去重置顶（每菜谱一条），详情打开自动记录，可删单条/清空。
- [x] **回收站**：软删除入回收站、`restore` 恢复、`?forever=true` 彻底删除（级联清理）、回收站内仍可搜索。
- [x] **每日菜单**：`meal_plans` 表 + `menu` API（单日/月份日期/瀑布流），当天食材+调料聚合为采购清单。
- [x] **发现推荐**：`discover` API，今日推荐（cooked×2+收藏×3+按日固定随机种子 + 随机探索）/热门/最新/随机。
- [x] **前端**：移动端底部 tabbar（菜谱库/菜单/发现/我的），菜谱库筛选面板（食材/匹配/分类/排序/状态）、菜谱编辑表单、详情增强（收藏/加菜单/编辑/删除）、菜单日历、发现页、收藏/历史/回收站/分类/调料/我的页面、首页推荐+搜索。补建 `tsconfig.json` 使 `npm run build`（vue-tsc）可执行。
- [x] **验收**：后端 `pytest tests/ -v` 121 用例全绿；前端 `npm run build` 通过；临时 SQLite 端到端冒烟（搜索/发现/菜单/收藏/历史）通过。
- [x] 规避 cook 坏点：不引入 view_count/favorite_count 死列；历史删单条；分类归属校验；`parent_id` 预留。

